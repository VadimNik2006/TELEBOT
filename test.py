# a = [{'id': 1, 'user_id': 847671391, 'film_id': 4395219, 'date': (2024, 7, 26, 15, 12, 37)}]
# for index, elem in enumerate(a):
#     print(index, elem)
#     for key, val in elem.items():
#         print(f"{key}: {val}"

a = []
for i in a:
    print("a")

    # pprint(db_controller.get_all_history())
    # if len(db_controller.get_all_history()) == 0:
    #     history_db(user_id=user_id, film_id=film_id)
    # else:
    #     for index, elem in enumerate(db_controller.get_all_history()):
    #         if film_id in elem.values():
    #             dict(db_controller.get_all_history()[index]).clear()
    #             history_db(user_id=user_id, film_id=film_id)
    #             pprint(db_controller.get_all_history())
    #             break
    #         else:
    #             history_db(user_id=user_id, film_id=film_id)
    #             break

    # for index, elem in enumerate(db_controller.get_all_history()):
    #     for key, val in elem.items():
    #         if key == "film_id" and film_id == val:
    #             print("in if")
    #             db_controller.get_all_history()[index] = history_db(user_id=user_id, film_id=film_id)
    #         else:
    #             print("in else")
    #             history_db(user_id=user_id, film_id=film_id)